# Cloudflare Pages deployment via Wrangler CLI
# Usage: npx wrangler pages deploy out --project-name=silent-circuit

name = "silent-circuit"
compatibility_date = "2025-01-01"

pages_build_output_dir = "out"

[env.production]
